# Twatlr

Easy Twitter threads.

### API

    (get-thread tweet-id) → list?
      tweet-id : string?

    (get-tweet tweet-id) → hash-eq?
      tweet-id : string?

    (tweet-url tweet-id) → url?
      tweet-id : string?
